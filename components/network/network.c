#include <stdio.h>
#include "network.h"
#include "esp_log.h"

#define TAG "network"
void init_network(void)
{
    ESP_LOGI(TAG, "Network initialized!");
    ESP_LOGI(TAG, "Network initialized 2!");
    ESP_LOGI(TAG, "Network initialized 3!");
}
